import { useCallback, useState, forwardRef } from "react";
import "./style.css";

function BlogPostDate3() {
  return (
    <div className={`date-container2`}>
      <div className={`date-container8`}>
        <p className={`date-label`}>12 Feb</p>
      </div>
    </div>
  );
}

export default BlogPostDate3;
