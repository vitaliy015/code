import { useCallback, useState, forwardRef } from "react";
import "./style.css";

function CreativeSection() {
  return (
    <div className={`creative-container`}>
      <p className={`creative-heading`}>we are creative</p>
    </div>
  );
}

export default CreativeSection;
