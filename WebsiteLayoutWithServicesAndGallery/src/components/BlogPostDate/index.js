import { useCallback, useState, forwardRef } from "react";
import "./style.css";

function BlogPostDate() {
  return (
    <div className={`date-container10`}>
      <div className={`date-container5`}>
        <p className={`date-label`}>12 Feb</p>
      </div>
    </div>
  );
}

export default BlogPostDate;
